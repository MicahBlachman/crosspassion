services:
  - type: web
    name: crosspassion
    runtime: node
    plan: free
    region: ohio
    autoDeploy: true
    buildCommand: "echo No build step"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: PORT
        value: 3000
